import {DisplayDataComponent} from "../src/features/display-data/display-data.component";

describe('display-data.cy.ts', () => {
  it('playground', () => {
    // cy.mount()
  })
})

cy.mount(DisplayDataComponent)

